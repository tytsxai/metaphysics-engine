# BaZi Master 任务清单

> 更新: 2026-07-29

## 当前状态

| 指标     | 状态                                                        |
| -------- | ----------------------------------------------------------- |
| 交付形态 | HTTP API + 程序化 CLI，无前端、无账号系统                   |
| 算法能力 | 八字/紫微/六爻/六壬/奇门/八宅/择吉/姓名/塔罗/易经/星座/合盘 |
| 运行形态 | 无状态纯计算，没有数据库                                    |
| 后端测试 | 475 项通过                                                  |
| CLI 契约 | 97 项通过                                                   |
| Lint     | 通过，无 warning                                            |
| 部署验证 | `scripts/verify-deployment.sh` 6 项全过                     |
| Redis    | 可选，纯缓存，缺失不影响结果                                |

> 数字请在改动后用 `./bazi test --json` 重新确认再更新，不要凭印象写。

## 待办

- [ ] `backend/services/ganzhi.service.js` 里 `matchPair` 定义了没用（ESLint warning）。
      确认是漏接还是残留，然后接上或删掉 —— 别让 lint 里长期挂着一条没人看的 warning。
- [ ] 庙旺利陷表：各家分歧且暂无可交叉验证的底本，当前不硬编。找到可靠底本再补，
      补的时候要跟其他流派分歧点一样就地标注选定口径。
- [ ] 地名表只有 33 个城市，认不出时静默跳过真太阳时校正（`chartTime.trueSolarTime` 为
      `null`）。真太阳时现在参与排盘了，这个静默跳过的代价比以前大 —— 要么扩表，
      要么让「认不出」在响应里更显眼。
- [ ] 后端覆盖率门槛：`test:coverage` 存在但无阈值，CI 也没跑
- [ ] Agent Benchmark 底座：成功率 / 平均步骤数 / 错误率的量化。**确定性这一层已经有了**
      （`bazi test engine` 会拿真实引擎验证每条声明可复现的命令连调两次一致，CI 里跑），
      缺的是任务级的基准用例。注意可复现性是逐条命令声明的，不是按 `calc` / `cast` 分组：
      `calc daily` 恒不可复现，`cast iching --numbers` 反而确定
- [ ] 部署时按所用 LB 查表设 `SHUTDOWN_DRAIN_MS`：机制已实测（+2ms 摘流、
      误差 10ms 量级，见 PRODUCTION.md 的对照表），但默认 5000 只够 nginx 被动检查；
      换 k8s 要 35000、ALB 要 65000，且都要同步抬高 `stop_grace_period`
- [ ] 按来源 IP 的限流目前只有应用层一层。公网部署时建议在自己的反向代理上再加一层，
      注意套了 CDN 时要设 `set_real_ip_from` / `real_ip_header`，否则会退化成全站共用一个桶
- [ ] TypeScript 迁移评估

## 已完成

### 能力接口（本轮）

- [x] `bazi schema`：从命令树导出 agent tool schema（anthropic / openai / mcp 三种形状），
      供上层 Runtime 的 Tool Registry 装载。纯本地生成，默认只导出 calc / cast
- [x] 必填改成声明式（`required` / `choices` / `variadic` 落到 flag 与 arg 的 spec 上），
      `parseArgs` 统一校验 —— 此前写在 run 里，导出的 schema 会声称"什么都不必填"

### 算法能力层（本轮）

- [x] 干支基础层：纳音、藏干、十二长生、合冲刑害破会、五行局、旬空。
      纳音 60 条与 lunar-javascript 逐条比对通过
- [x] 紫微安星重写 —— 原实现紫微星定位公式是编造的（无五行局概念），导致整盘皆错；
      补五行局、六吉六煞、大限、小限、流年
- [x] 八字断命层：藏干加权五行、身强身弱、扶抑法用神、神煞、四柱刑冲合会、
      起运时刻、逐年流年
- [x] 真太阳时真正参与排盘（此前只作元信息返回），缓存键同步纳入出生地/分钟/时区
- [x] 六爻纳甲装卦，并补上六十四卦真卦名 —— 原数据是程序生成的占位描述
- [x] 大六壬起课，三传九宗门全备
- [x] 奇门遁甲排局（拆补法定局、转盘法排盘）
- [x] 八宅风水、择吉历注、姓名五格
- [x] 确立能力边界：结构归引擎、断语归调用方（紫微庙旺、奇门格局不做，非缺口）

### 既有

- [x] 干支基础层（纳音/藏干/十二长生/合冲刑害破会/五行局/旬空），纳音与 lunar-javascript 逐条比对
- [x] 紫微安星重写（原实现紫微星定位错误导致整盘皆错），补五行局/六煞/大限/小限/流年
- [x] 八字断命层（藏干加权、旺衰、扶抑法用神、神煞、刑冲合会、起运时刻、逐年流年）
- [x] 真太阳时真正参与排盘，缓存键同步纳入出生地/分钟/时区
- [x] 六爻纳甲装卦，并补上六十四卦真卦名（原数据是程序生成的占位描述）
- [x] 大六壬起课，三传九宗门全备
- [x] 奇门遁甲排局（拆补法定局、转盘法排盘）
- [x] 八宅风水、择吉历注、姓名五格
- [x] 确立能力边界：结构归引擎、断语归调用方（庙旺利陷与奇门格局不做）
- [x] 后端模块化重构
- [x] 添加 LICENSE 文件
- [x] 生成 OpenAPI/Swagger 文档（`/api-docs`，生产 Basic Auth 保护），CI 守快照
- [x] 健康检查（`/live`、`/health`、`/api/ready`）
- [x] 根级 ESLint/Prettier 配置，并接入 CI
- [x] 错误追踪集成（Sentry，采样率/environment/release 可配）
- [x] `/metrics` Prometheus 端点，健康检查改用带缓存的快照
- [x] `./bazi` 程序化 CLI
- [x] 容器自愈（autoheal）
- [x] 优雅停机加排水窗口：SIGTERM 后 `/health` 和 `/api/ready` 立即 503，
      等 `SHUTDOWN_DRAIN_MS` 再关监听，避免滚动发布期间 LB 打进来的请求被拒
- [x] 排水时序端到端实测并写进 PRODUCTION.md（+2ms 摘流 / 误差 10ms 量级）
- [x] 容器 healthcheck 用 `/live` 而不是深度检查，避免依赖抖动经 autoheal 放大成崩溃循环
- [x] AI 流式响应加空闲超时（原来只有响应头有 deadline，中途卡住会永久挂住连接和并发槽）
- [x] 后端容器直接 `node scripts/start.mjs`，不再经 npm/sh 转发 SIGTERM
- [x] 容器 `ulimits.nofile` 显式钉死
- [x] 删除前端，项目收敛为纯算法能力层
- [x] 删除存储层与账号系统：数据库、Prisma、会话、OAuth、SMTP、历史记录、收藏、
      媒体接口全部移除，引擎变成无状态纯计算
- [x] 干支基础层（`constants/ganzhi.js` + `services/ganzhi.service.js`），八字与紫微共用；
      纳音 60 条与 lunar-javascript 逐条比对通过
- [x] 八字断命层：藏干加权五行、身强身弱、扶抑法用神喜忌、神煞、纳音、十二长生、
      四柱刑冲合会害破、空亡，大运补起运时刻与逐年流年
- [x] 紫微安星重写：此前紫微星定位是编造的（`月支 + (生日-1)`，没有五行局概念），
      导致整盘皆错。现按「命宫干支纳音定五行局 → 紫微 → 天府镜像 → 诸星」的正统链条，
      `ziwei_anxing.test.js` 逐日比对守着
- [x] 紫微闰月按「归本月」流派，修掉两个静默 bug（负数月份进索引导致月支错位、
      `isLeap` 恒为 false）
- [x] **真太阳时真正参与排盘**（此前只作为响应字段返回，排盘仍吃钟表时间），
      缓存键同步纳入出生地、分钟、时区与 `trueSolarTime` 开关
- [x] 清理随存储层失效的全部资料：备份/恢复/cron/故障演练脚本、postgres 初始化目录、
      四份运维文档、CLI 的 `db`/`verify` 命令树，以及 README/llms.txt/PRODUCTION 等
      文档里的对应描述
- [x] CLI 安全边界从「数据库破坏性操作」改挂到 `env init --force`（覆盖 `.env` 会丢掉
      本机 API Key），exit 7 契约仍然有效
- [x] 修掉 `scripts/verify-deployment.sh` 的三个既有 bug：日志写 stdout 污染响应体导致
      所有 JSON 校验恒假、`validate_json` 两参调用在 `set -u` 下中止全脚本、
      空数组展开在 bash 3.2 下报 unbound

## 已放弃

- WebAssembly 重计算逻辑 —— 前端 `assembly/` 只有三个未被任何代码调用的函数，
  却被 `predev`/`prebuild` 钉在构建关键路径上，是个纯粹的失败点。已整条移除。
- 自带 React 前端 —— 界面形态因产品而异（Web / 小程序 / App / 纯 agent 调用），
  塞一套参考实现进来只会模糊能力层的边界，还要为它维护 91 个 Playwright 用例和
  一整套浏览器依赖。已整体删除，界面交给调用方。
- 账号系统（注册/登录/OAuth/密码重置/历史记录/收藏）—— 和命理算法没有任何关系，
  任何真实产品都会用自己那一套。留着它意味着这个项目要为一堆自己不擅长、调用方也不会用的
  能力承担安全和维护责任。删掉之后整个存储层、会话层，以及它们带来的备份/迁移/一致性
  问题一起消失。
- WebSocket AI 流式输出（`/ws/ai`）—— 随账号系统一起移除。需要流式的调用方可以在
  自己那一层实现，或者提 issue 讨论重新引入一个无状态版本。
