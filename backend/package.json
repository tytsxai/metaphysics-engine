{
  "name": "bazi-master-backend",
  "private": true,
  "version": "0.2.0",
  "type": "module",
  "description": "Stateless HTTP surface over the BaZi / Zi Wei Dou Shu / Tarot / I Ching / Zodiac / Synastry calculation engine, with OpenAPI, health checks and optional AI providers.",
  "license": "UNLICENSED",
  "homepage": "https://github.com/tytsxai-stack/metaphysics-engine#readme",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/tytsxai-stack/metaphysics-engine.git",
    "directory": "backend"
  },
  "bugs": {
    "url": "https://github.com/tytsxai-stack/metaphysics-engine/issues"
  },
  "engines": {
    "node": ">=20",
    "npm": ">=9"
  },
  "scripts": {
    "dev": "node server.js",
    "start": "node scripts/start.mjs",
    "test": "node --test --test-concurrency=1 test/*.test.js",
    "test:coverage": "node --experimental-test-coverage --test-coverage-exclude=test/** --test --test-concurrency=1 test/*.test.js",
    "test:coverage:gate": "node --experimental-test-coverage --test-coverage-exclude=test/** --test-coverage-lines=91 --test-coverage-branches=82 --test-coverage-functions=90 --test --test-concurrency=1 test/*.test.js"
  },
  "dependencies": {
    "@sentry/node": "^10.32.1",
    "@sentry/profiling-node": "^10.32.1",
    "compression": "^1.8.1",
    "cors": "^2.8.5",
    "express": "^4.21.1",
    "helmet": "^7.1.0",
    "lunar-javascript": "^1.7.7",
    "pino": "^10.1.0",
    "pino-http": "^11.0.0",
    "redis": "^4.6.14",
    "swagger-ui-express": "^5.0.0"
  },
  "devDependencies": {
    "supertest": "^7.1.4"
  }
}
